---
name: crux-on
description: Arme le gate Crux pour cette session (code, plan ou both).
---
Lance `crux on $ARGUMENTS` (defaut : `code`), puis annonce a l'utilisateur le
mode arme et sa source, en lancant `crux status`.
